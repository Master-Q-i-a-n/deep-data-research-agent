bind 0.0.0.0 ::
protected-mode yes
port 6379
dir /data

# Durable enough for short-lived limiter state while keeping write latency low.
appendonly yes
appendfsync everysec
save 3600 1 300 100 60 10000

maxmemory 256mb
maxmemory-policy noeviction

aclfile /run/redis/users.acl
